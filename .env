# .env file for solar-monitoring-api

# IMPORTANT: Pydantic BaseSettings expects actual values for environment variables.
# For local development, you can set them here.
# For production, these should be set in the environment itself.

SECRET_KEY="your_secret_key_here_please_change_me"

POSTGRES_SERVER="localhost"
POSTGRES_USER="your_db_user"
POSTGRES_PASSWORD="your_db_password"
POSTGRES_DB="solar_db"

# SQLALCHEMY_DATABASE_URL="postgresql://user:password@server/db"
# If SQLALCHEMY_DATABASE_URL is set, it will be used directly.
# Otherwise, it will be assembled from the POSTGRES_* variables above.
# Example for a Docker setup might be:
# SQLALCHEMY_DATABASE_URL="postgresql://solar_user:solar_password@postgres_db/solar_db_prod"
